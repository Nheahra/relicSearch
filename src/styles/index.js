import { makeStyles, createStyles } from '@mui/styles'

export const FarmGuideStyles = makeStyles(() => createStyles({
  systemDetails: {
    '&:hover': {
      color: 'blue',
      cursor: 'pointer',
    },
  },
}))
